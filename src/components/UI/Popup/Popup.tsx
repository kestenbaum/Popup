import {FC} from "react";

import styles from "./Popup.module.css"
const Popup:FC = () => {
    return (
        <section
            className={styles.wrapper}
        >

        </section>
    );
};

export default Popup;